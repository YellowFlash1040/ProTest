import {
  TOKEN_KEY,
  REFRESH_TOKEN_KEY,
  SID_KEY,
  ANSWERS_KEY,
  TEST_TYPE_KEY,
} from "./localStorage/localStorage"
import { API_HOST, GOOGLE_AUTH_ROUTE } from "./api/api"

export {
  API_HOST,
  GOOGLE_AUTH_ROUTE,
  TOKEN_KEY,
  REFRESH_TOKEN_KEY,
  SID_KEY,
  ANSWERS_KEY,
  TEST_TYPE_KEY,
}
